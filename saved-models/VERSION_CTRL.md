# Version Control Convention

## Document Description
Versions are named in respect to *parent version*, (ex. v0.1.0), and *subversion*, (ex. v0.1.1), and are ordered in similarity of model. The deeper you get into directories the more similar the models get (eg. the smaller the changes are to each model). <br/>

## Models